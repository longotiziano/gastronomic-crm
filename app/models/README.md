# IMPORTANT

Every column table that has an image, it is important to name it `image_url`, because of the `**kwargs` functions. If you want to put another name, please specify it in the module `app/repositories/base_repository.py` and go to the `create_recourd()` function.